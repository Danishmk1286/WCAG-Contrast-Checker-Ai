/**
 * Blog Export Script - REMOVED (Zero File Output Policy)
 * 
 * This script previously exported blogs to blog_export.json.
 * File writes have been removed per zero file output policy.
 * 
 * Use database backup tools for backups instead.
 */

console.log('⚠️  Blog export script disabled - zero file output policy');
console.log('   Use database backup tools for backups');
console.log('   Blog data is available via API: /api/blog/posts');
process.exit(0);
